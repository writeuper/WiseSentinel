# 阶段 162：RAG 恢复采样与 P95 精度优化

## 运行态恢复

Milvus 服务恢复健康后，重新执行 5 次真实知识库闭环（上传、索引、RAG Citation、版本校验、删除）：

- 5/5 检索成功
- 5/5 高置信度
- 0 次 RAG 检索错误
- 检索平均耗时约 201ms
- 当前 histogram P95 为 500ms 桶上界，样本量仅 5，仍属于开发/集成基线，不是生产 SLA

RAG 库存当前为 24 篇 active 文档、87 个 published Chunk、5 篇 legacy 文档、207 个物理向量。

## 指标优化

根据真实样本主要分布在 200–400ms，细化 `ws_rag_retrieval_duration_seconds` 的子秒桶，新增 `350ms/400ms/450ms` 边界，减少 P95 被粗粒度 `500ms` 桶上界夸大的误差。

新增 histogram 精度单测，确保后续改动不会删除这些子秒桶。

## 质量口径

- RAG P95 仍应标注为 histogram bucket upper bound。
- 少于 30 个样本时不得作为生产 SLO；持续采样达到 30+ 后再作为准入/告警依据。
- Milvus 先前出现的 Flush 超时已被 durable retry/dead-letter 正确记录；恢复后新闭环成功，说明失败主要来自依赖瞬时不稳定而非固定业务逻辑错误。
