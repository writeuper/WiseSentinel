# 阶段 169：全量门禁与一致性回归

## 本轮变更

- VectorGC 抢占后重载失败进入持久化重试/死信，避免任务长期悬挂在 `running`（阶段 167）。
- Approval 决定流程向上返回 `RowsAffected()` 错误，避免数据库异常被误报为普通未命中（阶段 168）。

## 门禁结果

| 门禁 | 结果 |
| --- | --- |
| `go test ./...` | 通过 |
| `go vet ./...` | 通过 |
| 关键包 `go test -race ./internal/orchestrator/task ./internal/repository ./internal/gateway/handler` | 通过 |
| Python 自动化测试 | 50/50 通过 |
| 敏感信息 Sink 检查 | 通过 |
| `git diff --check` | 通过 |
| Compose 配置校验 | 通过 |
| Portal `npm run build` | 通过 |

Portal 构建仍提示 antd vendor chunk 约 1,047.85 KiB，属于性能优化告警，不影响本次构建成功；入口业务 chunk 约 28.51 KiB。

## 指标口径与限制

本轮门禁验证代码和契约，不重新生成真实模型/RAG 线上样本，因此不覆盖此前已采集的 Agent、RAG 和并发基线。真实 P95、任务完成率、工具命中率必须以运行态 Trace/Prometheus 和独立租户评测脚本为准；未配置 `OPS_TEST_MYSQL_DSN` 或外部服务时，集成测试不宣称真实数据库成功。
