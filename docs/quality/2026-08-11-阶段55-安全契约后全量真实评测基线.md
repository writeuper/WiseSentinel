# 阶段 55：安全契约后全量真实评测基线

日期：2026-08-11

## 目的

阶段 49–52 将工具正文替换为安全投影、来源 allowlist 与受控诊断表达。定点用例通过不足以证明整体无回归，因此重新在已启动的真实模型、RAG、MySQL、Redis、Milvus、Prometheus 环境执行完整 HTTP 评测集。

## 真实执行结果

命令：

```bash
python3 scripts/run_agent_eval.py --base-url http://127.0.0.1:8090/api/v1 ...
```

| 指标 | 结果 |
| --- | --- |
| 可执行评测集 | 24 场景 |
| 任务通过率 | 24/24（100%） |
| 路由准确率 | 100% |
| 必需工具调用成功率 | 25/25（100%） |
| 关键词原始命中 | 56/68（82.4%） |
| P50 / P95 响应时间 | 253ms / 22.99s |

关键词指标低于 100% 不代表场景失败：评测按场景的最低命中阈值判定，24 个场景均满足。工具正确性仍以持久 Trace 的工具名/状态验证，来源以安全 allowlist metadata 验证，未读取工具正文。

## 结论与风险

- 安全投影切换后，评测功能、路由、工具和 RAG 闭环均恢复到 100% 场景通过。
- P95 仍受真实模型/RAG 长尾影响，不能据此宣称生产 SLO 达标；应把 Chat、Ops、RAG、provider 调用分桶，并增加超时/重试/队列指标。
- 当前仍为开发环境真实依赖与模型 key 验证，不包含真实生产用户流量、生产峰值或生产 RBAC 身份矩阵。

评测工件：`/tmp/wisesentinel-agent-eval-stage55.csv` 与 `/tmp/wisesentinel-agent-eval-stage55-summary.json`（临时运行产物，不入库）。
