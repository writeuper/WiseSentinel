# 阶段 109：Ops 全量回归与日志优先级修复

## 修复后 85 条 Ops 评测

- 通过率：**84/85=98.8%**
- 路由准确率：**100%**
- 工具成功率：**99.1%（113/114）**
- 关键词命中率：**99.1%（327/330）**
- P50：**1.998 秒**
- P95：**2.029 秒**
- 失败：仅 `LOG-014`，证据源缺失

相比初始基线 56/85=65.9%，通过率提升 **32.9 个百分点**；初始 6 条限流和 3 条禁止工具违约均已消除。

## 剩余问题与修复

`LOG-014` 同时包含“同步延迟”和“请检索日志”。旧规则先按指标词路由，调用了 `query_metric_range`，与用户显式日志意图不一致。

修复 focused tool 优先级：

1. 显式禁止工具；
2. 明确多来源组合；
3. 明确日志检索意图；
4. 指标语义；
5. 其他故障信号。

新增定向单测，要求该 Case 只调用 `search_logs`。

## 证据边界

本轮结果是本地开发/集成环境、固定评测集、串行间隔 2 秒的 HTTP 实测，不是生产 SLA。Chat 长尾和真实生产用户流量仍需独立统计。
