# 阶段 148：跨层门禁与指标采集回归

## 验证范围

本轮针对近期 Worker 生命周期和 Webhook 补偿改动，执行前端、后端、迁移、Compose 和可观测性跨层回归。

## 结果

| 门禁 | 结果 | 证据 |
|---|---|---|
| Portal TypeScript/Vite 生产构建 | 通过 | `npm run build` |
| Portal 初始入口包体 | 通过 | `test_portal_bundle.py`；entry 约 28.5 KB |
| Compose resilience | 3/3 通过 | etcd/MinIO/Milvus restart 和健康依赖契约 |
| Migration ledger | 通过 | 迁移脚本连续执行两次，全部 checksum verified |
| Prometheus 采集 | 通过 | target `wisesentinel-platform` 为 `up`，成功采集 `ws_http_requests_total` |
| Go 全量测试 | 通过 | `go test ./...` |
| Go race 关键包 | 通过 | bootstrap/task/repository/observability |
| Go vet | 通过 | `go vet ./...` |
| Python 自动化 | 50/50 | unittest 全量通过 |
| 敏感信息静态检查 | 通过 | `check_sensitive_sinks.sh` |

## 真实 Chat 契约与并发

- 同步 Chat 幂等回放：通过；相同 `Idempotency-Key` 返回一致响应投影，冲突 payload 被拒绝。
- 同键并发提交：通过；结果为一成功一冲突，并复用同一 Trace。
- 4 请求 / 2 并发真实 Chat：4/4 成功，会话清理 4/4 验证；P50 约 7.12 秒，P95 约 17.58 秒。
- 本轮模型调用 4 次，准入拒绝 0 次；平均模型调用累计耗时约 9.55 秒/次。该值为本地真实模型环境观测，不作为生产 SLO 承诺。

## 运行态指标

- `/health/ready` 所有依赖为 `up`，包括 `alert_event_reaper`、Ops/Index/VectorGC Worker。
- Prometheus target：`job=wisesentinel-platform`，health=`up`。
- 已验证 Prometheus 查询返回 `ws_http_requests_total`，HTTP 200 的 `/metrics` 与 `/health/ready` 样本均已入库。
- `ws_alert_event_orphan_reaped_total` 当前为 0，表示本轮启动后没有新增孤儿预留回收。

## 包体与剩余风险

Portal 初始入口满足 500 KB 门禁；Vite 仍提示 `antd` vendor chunk 约 1.05 MB（gzip 约 327 KB）。这不影响当前入口包体或功能回归，但建议后续按页面/组件继续拆分 vendor，并以真实首屏 LCP、缓存命中率和网络条件进行容量评估。

## 关联资产

- [阶段147：Webhook 并发幂等与部署迁移修复](./2026-08-13-阶段147-Webhook并发幂等与部署迁移修复.md)
- [阶段145：完整 130 条分层评测](./2026-08-13-阶段145-完整130条分层评测与模型长尾复核.md)
