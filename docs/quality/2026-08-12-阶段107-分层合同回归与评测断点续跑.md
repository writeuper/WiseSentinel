# 阶段 107：分层合同回归与评测断点续跑

## 本轮问题

全量 130 条在线评测中，模型长尾或 admission rejection 可能导致单个 Case 长时间等待，原脚本被中断后无法安全续跑，且失败结果不易区分是平台合同问题还是上游模型容量问题。

## 代码优化

- `scripts/run_agent_eval.py` 新增 `--resume`：每完成一个 Case 即落盘；重启后跳过已通过 Case，自动重试之前失败 Case。
- 新增 `scripts/test_run_agent_eval_resume.py`，验证结果读取和失败重试策略。

## 真实回归证据

- Ops 断点续跑测试：首次 5/5 通过；第二次同一输出文件跳过 5 个已通过 Case。
- Trace 访问合同：通过。
- 并发 Chat 3 请求、并发 2：3/3 成功、cleanup 3/3；期间出现 2 次模型 admission rejection，最终成功率仍为 100%，应作为容量压力指标单独观察。
- 低负载幂等回放：通过，返回 `idempotency_replay_success`。
- 低负载 SSE basic：通过，返回 `success_done`。
- 高负载期间 SSE/幂等曾出现 `api_50304` 和 `sse_50304_retry_2`；降载后恢复，归因于模型 admission 容量压力，未发现必现的合同实现错误。

## 当前质量结论

- 平台基础服务健康：MySQL、Redis、Milvus、etcd、MinIO healthy，后端 ready=true。
- RAG 10 份 Runbook 已全部上传索引成功，RAG 成功检索 P95 约 250ms。
- 评测执行器现在支持长批次断点续跑，但 130 条全量在线评测仍需按 Chat/Ops/RAG 分层、固定并发和时间窗执行。

## 剩余风险

- 模型准入 rejection 在并发 Chat 下仍可见，需要按供应商、并发级别和 admission counter 建立容量门禁。
- 低负载合同通过不等价于高并发 SLA；生产准入仍需 100+ 请求、固定模型版本和明确 P95 阈值。
- 当前无真实生产用户流量证据。
