# 阶段 139：130 条真实评测结果

## 执行范围

使用冻结的 130 条企业级评测集执行真实 HTTP 测试：

- Ops：85 条，覆盖告警、日志、指标、Trace、发布、多工具、权限和依赖降级；
- Chat/RAG：45 条，覆盖平台能力问答、20 条 Runbook Citation、10 条时间工具。

运行时结果只保存在 `/tmp`，仓库不保存模型原文、Trace 内容、用户问题或凭证。

评测脚本默认输入已切换为 `enterprise_agent_eval_cases_130.csv`；旧的 27 条 CSV 仅作为兼容样本保留。

## 汇总结果

- 总体通过率：123/130 = 94.62%；
- 业务质量通过率：123/123 = 100%；
- 业务路由准确率：100%；
- 业务工具命中率：100%（131/131）；
- 业务关键词命中率：95.24%（420/441）；
- 全部 Case 延迟 P50/P95：166ms / 34.90s；
- 基础设施失败率：7/130 = 5.38%。

失败分布：

- 6 条 `rate_limited`：来自第一轮 Ops 批次在旧服务进程下触发 60 秒滑动窗口；已修复 Retry-After 契约，不能归因于 Agent 质量；
- 1 条 `timeout`：RAG-010 触发模型 504，约 40.1s；模型指标记录 1 次 timeout，成功模型调用 P95 约 30s。

## RAG 与规模指标

- RAG 检索成功样本：62；
- RAG 成功均值：约 183ms；
- RAG 成功 P95：250ms histogram bucket upper bound；
- 活跃文档：24；当前发布 Chunk：78；Legacy 文档：11；物理向量：约 198；
- 模型调用：61 次，其中成功 60、超时 1；成功均值约 10.0s，成功 P95 约 30s。

## 结论

当前平台在本地真实依赖和模型 API 下，业务路由、工具选择和 Citation 断言均通过；主要剩余风险是模型长尾与单次 30 秒超时预算。生产评测应继续按业务质量/基础设施质量拆分，并针对模型超时配置降级、重试预算和告警阈值。
